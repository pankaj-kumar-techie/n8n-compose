# Domain Configuration (Production Only - Required for HTTPS)
DOMAIN_NAME=n8n.example.com
SSL_EMAIL=user@example.com

# n8n Configuration
# For local setup, you can leave these as defaults or change them if you like.
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=secure_password_please_change
GENERIC_TIMEZONE=Asia/Kolkata
TZ=Asia/Kolkata

# Security
# WARNING: If you change this key after the first run, n8n will fail to start.
# If that happens, you must delete the volumes: docker volume rm n8n-compose_n8n_data
N8N_ENCRYPTION_KEY=n8n_local_secret_key_change_only_if_fresh_install

# Database Configuration (Postgres)
POSTGRES_USER=n8n
POSTGRES_PASSWORD=n8n_db_password_change_me
POSTGRES_DB=n8n
